{
  "name": "morphai-portfolio",
  "version": "1.0.0",
  "description": "MORPHAI Exhibition Designer Portfolio — Cloudflare Pages",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "wrangler pages dev . --binding MORPHAI_KV=test",
    "deploy": "wrangler pages deploy . --project-name=morphai"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
